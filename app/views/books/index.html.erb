<h1>Bookers</h1>
<% @books. each do |book| %>
<%= book.title %>
<%= book.body %><br>
<%= linl_to 'show', book %>
<% end %>

<h2>New book</h2>
<%= form_for @book do |f| %>
<h4>title</h4>
<%= f.text_field :title %>
<h4>body</h4>
<%= f.text_area :body %>

<%= f.submit 'Create Book' %>
<% end %>